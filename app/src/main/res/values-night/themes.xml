<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.Maratona" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Cor do botão ou elementos principais no modo escuro -->
        <item name="colorPrimary">@color/primaria</item>
        <item name="colorOnPrimary">@android:color/white</item>

        <!-- Cor de fundo da janela para o modo escuro -->
        <item name="android:windowBackground">@color/escura</item>

        <!-- Cor para textos secundários, se necessário -->
        <item name="colorSecondary">@color/cinza</item>
    </style>
</resources>
